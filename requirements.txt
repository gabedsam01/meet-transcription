requests>=2.32,<3
fastapi>=0.115,<1
starlette>=0.46,<1
uvicorn[standard]>=0.32,<1
jinja2>=3.1,<4
python-multipart>=0.0.12,<1
cryptography>=43,<46
bcrypt>=4,<5
itsdangerous>=2.2,<3
httpx>=0.28,<1
google-api-python-client>=2.150,<3
google-auth>=2.36,<3
google-auth-httplib2>=0.2,<1
google-auth-oauthlib>=1.2,<2

# Database: PostgreSQL via SQLAlchemy (sync) + Alembic migrations.
sqlalchemy>=2.0,<3
alembic>=1.13,<2
psycopg[binary]>=3.2,<4
passlib[bcrypt]>=1.7,<2

# Redis: transcription queue + global execution lock (QUEUE_BACKEND=redis).
redis>=5,<6

# NOTE: local transcription engines are NOT installed here — they are heavy and
# CPU/arch-specific. faster-whisper is installed at image build time only when
# INSTALL_FASTER_WHISPER=true (see Dockerfile build args); whisper.cpp is provided
# via an external WHISPER_CPP_BINARY. See docs/architecture/local-transcription.md.

pytest>=8,<9
